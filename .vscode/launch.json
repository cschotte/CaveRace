{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug CaveRace",
            "type": "lldb",
            "request": "launch",
            "program": "${workspaceFolder}/1.5 Odin (Windows & MacOS)/build/caverace",
            "cwd": "${workspaceFolder}/1.5 Odin (Windows & MacOS)/source",
            "preLaunchTask": "odin: build debug",
            "terminal": "integrated",
            "stopOnEntry": false
        }
    ]
}
