{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug CaveRace",
            "type": "lldb",
            "request": "launch",
            "program": "${workspaceFolder}/build/caverace",
            "cwd": "${workspaceFolder}/source",
            "preLaunchTask": "odin: build debug",
            "terminal": "integrated",
            "stopOnEntry": false
        }
    ]
}
